exit
bucket.files.get(png_path_generate(@resume.image_hash)).destroy
bucket.files.get(png_path_generate(@resume.image_hash))
png_path_generate(@resume.image_hash)
png_path_generate(@image_hash)
bucket.files.get("images/1/84fd0d6dc824bb.png")
bucket.files.get(png_path_generate(@image_hash))
@resume.image_hash
@resume.hash
@resume
png_path_generate(hash)
bucket.files.get(png_path_generate(hash))
bucket.files
